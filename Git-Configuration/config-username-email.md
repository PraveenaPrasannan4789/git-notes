 # Configure Git Username & Email

•	Git uses your name and email to identify commits.
•	Set globally (for all repos) once:
git config --global user.name "Praveena Prasannan"
git config --global user.email "your.email@example.com"

