# Verify configuration:
git config --global user.name
git config --global user.email
•	If configured, output will show:
Praveena Prasannan
praveena@example.com
•	If nothing is shown → not configured yet.

